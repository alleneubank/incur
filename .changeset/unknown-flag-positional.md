---
'@alleneubank/incur': patch
---

An unknown `--<name>` flag that matches a positional argument says so (`Unknown flag: --channel; channel is positional: <channel> <ts>`) instead of only `Unknown flag: --channel`.
