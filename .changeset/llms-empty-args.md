---
'@alleneubank/incur': patch
---

Commands with an empty `args` schema no longer get a trailing space in the `--llms` command table. In `--llms-full` and generated skills, empty `args`, `env`, and `options` schemas no longer render header-only tables.
