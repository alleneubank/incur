---
'@alleneubank/incur': patch
---

Command `--help`, and the usage printed after a validation error, list the framework-injected options (`--dry-run` for mutating commands, `--json` for commands with a payload, `--page-size` for paginated commands). `--schema` already listed them.
